{
  "name": "flamegraph-analytics",
  "private": true,
  "description": "Flamegraph analytics demo (mock data by default; optional BigQuery backend)",
  "scripts": {
    "start": "npm run start --prefix local",
    "serve": "npm run serve --prefix local",
    "test": "npm test --prefix local"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
